# إعداد شاشة البداية (Splash Screen) - PlayControl

## الخطوة 1: إضافة ملف الشعار

### 1.1 إنشاء مجلد الأصول
قم بإنشاء مجلد `assets` في مجلد `android/app/src/main/res/` إذا لم يكن موجوداً:
```
android/app/src/main/res/assets/
```

### 1.2 إضافة الشعار
ضع ملف الشعار الخاص بك في مجلد الأصول:
- يجب أن يكون الملف بصيغة PNG
- يجب أن يكون الخلفية شفافة
- الحجم الموصى به: 512x512 بكسل

## الخطوة 2: إعداد ملف pubspec.yaml

أضف الأصول إلى ملف `pubspec.yaml`:

```yaml
flutter:
  uses-material-design: true
  
  assets:
    - assets/logo.png
    - assets/icons/
```

ملاحظة: إذا كان لديك مجلد `assets` في جذر المشروع، يمكنك إضافته أيضاً:
```yaml
flutter:
  assets:
    - assets/
```

## الخطوة 3: التحديث الديناميكي

بعد إضافة الأصول، قم بتشغيل الأمر التالي لتحديث التطبيق:
```bash
flutter pub get
```

## الخطوة 4: تخصيص الشعار

في ملف `splash_screen.dart`، يمكنك تخصيص الشعار عن طريق تغيير:

### 4.1 تغيير الأيقونة
في دالة `_buildLogo()`، يمكنك تغيير الأيقونة من `Icons.videogame_asset` إلى أيقونة أخرى:
```dart
child: const Icon(
  Icons.your_custom_icon, // استبدل بأيقونتك المخصصة
  size: 60,
  color: Colors.white,
),
```

### 4.2 استخدام صورة مخصصة
إذا كنت تريد استخدام صورة مخصصة بدلاً من الأيقونة:
```dart
child: Image.asset(
  'assets/logo.png', // تأكد من إضافة الصورة إلى assets
  width: 80,
  height: 80,
),
```

## الخطوة 5: تعديل الألوان والتأثيرات

### 5.1 تعديل الألوان
يمكنك تعديل التدرج اللوني في `Container` الرئيسي:
```dart
gradient: LinearGradient(
  begin: Alignment.topCenter,
  end: Alignment.bottomCenter,
  colors: [
    Color(0xFF1A1A2E), // لون الأعلى
    Color(0xFF16213E), // لون الأوسط
    Color(0xFF0F0F23), // لون الأسفل
  ],
),
```

### 5.2 تعديل تأثير النيون
يمكنك تعديل ألوان النيون في `_buildTitle()`:
```dart
colors: [
  Color(0xFF6C63FF), // لون النيون الأول
  Color(0xFF00D4FF), // لون النيون الثاني
  Color(0xFFFFD700), // لون النيون الثالث
],
```

## الخطوة 6: تعديل النصوص

### 6.1 تغيير العنوان الرئيسي
```dart
Text(
  'PlayControl', // غير هذا النص
  style: TextStyle(
    fontSize: 32, // حجم الخط
    fontWeight: FontWeight.w900, // سماكة الخط
    letterSpacing: 2, // المسافة بين الحروف
  ),
),
```

### 6.2 تغيير العنوان الفرعي
```dart
Text(
  'ArcadeManager', // غير هذا النص
  style: TextStyle(
    fontSize: 18, // حجم الخط
    fontWeight: FontWeight.w700, // سماكة الخط
    letterSpacing: 3, // المسافة بين الحروف
  ),
),
```

## الخطوة 7: تعديل مدة شاشة البداية

يمكنك تغيير مدة شاشة البداية في `_SplashScreenState`:
```dart
_progressController = AnimationController(
  duration: const Duration(seconds: 3), // غير عدد الثواني
  vsync: this,
);
```

## الخطوة 8: التجربة

### 8.1 التجربة على المتصفح
```bash
flutter run -d chrome
```

### 8.2 التجربة على المحاكي
```bash
flutter run
```

### 8.3 بناء APK
```bash
flutter build apk --release
```

## المميزات المضمنة

### 1. **خلفية متدرجة ديناميكية**
- تدرج من الكحلي إلى البنفسجي الداكن
- متجاوب مع جميع أحجام الشاشات

### 2. **شعار نيون متوهج**
- تأثير نيون متعدد الألوان
- ظلال متوهجة حول الشعار

### 3. **نصوص نيون متوهجة**
- عنوان "PlayControl" متوهج بالألوان
- عنوان "ArcadeManager" باللون الذهبي

### 4. **شريط تقدم ديناميكي**
- شريط تقدم متحرك من 0% إلى 100%
- نص "Initializing..." في الأسفل
- عرض النسبة المئوية

### 5. **أيقونات خلفية زخرفية**
- أيقونات شبح ومفاتيح وماس على الخلفية
- خطوط دوائر كهربائية
- تأثيرات شفافة

### 6. **متجاوب بالكامل**
- يعمل على جميع أحجام الشاشات
- يدعم RTL للغة العربية
- يدعم الوضع الداكن

## استكشاف الأخطاء

### مشكلة: الأصول لا تظهر
**الحل:**
1. تأكد من إضافة مجلد `assets` إلى `pubspec.yaml`
2. قم بتشغيل `flutter pub get`
3. أعد تشغيل التطبيق

### مشكلة: الألوان لا تظهر بشكل صحيح
**الحل:**
تأكد من أن جميع الألوان بتنسيق 16-bit (مثل `0xFF123456`)

### مشكلة: الأيقونات لا تظهر
**الحل:**
تأكد من استخدام أيقونات متاحة في `Icons.` أو استخدم صور مخصصة

## ملاحظات إضافية

- تم تعيين الديناميكية بالكامل للتحريك
- تستخدم `LayoutBuilder` للمجاوبة
- تستخدم `Stack` و `Positioned` للعناصر الخلفية
- دعم للغة العربية (RTL)
- دعم للوضع الداكن
